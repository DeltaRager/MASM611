DATA SEGMENT
NUM1 DB 07H
NUM2 DB 02H
RES DB ?
DATA ENDS

ASSUME CS: CODE, DS: DATA
CODE SEGMENT
START:
        MOV AX, DS
        MOV DS, AX
        MOV AL, NUM1
        SUB AL, NUM2
        MOV AH, 00H
        MOV RES, AL
        HLT
CODE ENDS
END _START

